"""Supabase persistence adapter placeholder.

This module will implement repository interfaces using Supabase after
the assessment contract and table design are approved.

It must not:

* initialize a Supabase client
* read secret environment values
* create or migrate tables
* leak vendor types into `app.domain`
"""
